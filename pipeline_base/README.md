# Pipeline Base

This directory is a seed template for the pipeline branch.
It is source template stored on main. It is not an output artifact.

The orchestrator copies this tree and applies a Gemini manifest to produce a pipeline snapshot.
